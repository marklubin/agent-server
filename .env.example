# =============================================================================
# Kairix Agent Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Required values are marked with (REQUIRED)
# Optional values have defaults shown in comments

# -----------------------------------------------------------------------------
# API Keys (REQUIRED)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-api03-...
DEEPGRAM_API_KEY=...

# Optional - only if using OpenAI provider
# OPENAI_API_KEY=sk-proj-...

# -----------------------------------------------------------------------------
# Letta Configuration (REQUIRED for voice server)
# -----------------------------------------------------------------------------
# The agent ID to use for voice conversations
LETTA_AGENT_ID=agent-...

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Password for the Kairix Postgres database
# Default: kairix (used in docker-compose)
# POSTGRES_PASSWORD=kairix

# Full database URL - usually set automatically by docker-compose
# Default for docker: postgresql+asyncpg://kairix:kairix@kairix-postgres:5432/kairix
# Default for local:  postgresql+asyncpg://kairix:kairix@localhost:5432/kairix
# DATABASE_URL=postgresql+asyncpg://kairix:kairix@kairix-postgres:5432/kairix

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis URL for task queue and pub/sub
# Default for docker: redis://kairix-redis:6379
# Default for local:  redis://localhost:6379
# REDIS_URL=redis://kairix-redis:6379

# -----------------------------------------------------------------------------
# Letta Server URL
# -----------------------------------------------------------------------------
# URL of the Letta server (AI agent platform)
# Default for docker: http://letta:8283
# Default for local:  http://localhost:9000
# LETTA_BASE_URL=http://letta:8283

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR
# Default: INFO (use DEBUG for development)
# LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Session Detection
# -----------------------------------------------------------------------------
# Minutes of silence before starting a new session
# Default: 5
# SESSION_GAP_MINUTES=5

# -----------------------------------------------------------------------------
# Podman Compatibility (Linux only)
# -----------------------------------------------------------------------------
# If using Podman instead of Docker, set this to the Podman socket path
# Uncomment and set for Podman:
# DOCKER_SOCK=/run/user/1000/podman/podman.sock
#
# Or use the XDG variable (recommended):
# DOCKER_SOCK=$XDG_RUNTIME_DIR/podman/podman.sock
